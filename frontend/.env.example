# Optional explicit API URL for all environments.
# Example: http://localhost:8000
VITE_API_BASE_URL=

# Vite dev-server proxy target for /api requests in development mode.
# Used only when VITE_API_BASE_URL is not set.
VITE_BACKEND_URL=http://localhost:8000